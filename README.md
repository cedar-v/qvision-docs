# License Manager Docs

简洁的 VitePress 文档站点，记录 License Manager 的使用指南与 API 说明。

## 本地快速启动

1. 安装依赖（首次运行需要）  
   `npm install`
2. 启动文档服务器（默认端口 5173）  
   `npm run docs:dev`
3. 打开浏览器访问 `http://localhost:5173` 预览。

> 如需构建静态站点，可执行 `npm run docs:build`，产物位于 `docs/.vitepress/dist`。


## 添加文档目录操作说明

### 目录结构说明

```
docs/
├── index.md                 # 中文主页
├── en/
│   └── index.md            # 英文主页
├── guide/                  # 中文指南目录
│   ├── index.md           # 指南首页
│   └── getting-started.md # 快速开始
└── en/guide/              # 英文指南目录
    ├── index.md          # 英文指南首页
    └── getting-started.md # 英文快速开始
```

### 添加新文档步骤

1. **创建目录结构**
   ```bash
   # 在中文文档目录下创建新分类
   mkdir -p docs/guide/new-feature

   # 在英文文档目录下创建对应分类
   mkdir -p docs/en/guide/new-feature
   ```

2. **创建文档文件**
   ```bash
   # 创建中文文档
   touch docs/guide/new-feature/index.md
   touch docs/guide/new-feature/usage.md

   # 创建英文文档
   touch docs/en/guide/new-feature/index.md
   touch docs/en/guide/new-feature/usage.md
   ```

3. **配置导航菜单**

   编辑 `docs/.vitepress/config.mjs` 文件，在 `themeConfig.nav` 中添加新菜单项：

   ```javascript
   nav: [
     { text: '指南', link: '/guide/' },
     { text: '新功能', link: '/guide/new-feature/' }, // 添加新菜单
   ]
   ```

4. **配置侧边栏**

   在 `docs/.vitepress/config.mjs` 的 `themeConfig.sidebar` 中添加新侧边栏：

   ```javascript
   sidebar: {
     '/guide/new-feature/': [
       { text: '新功能介绍', link: '/guide/new-feature/' },
       { text: '使用方法', link: '/guide/new-feature/usage' }
     ]
   }
   ```

### 文档编写规范

1. **文件命名**: 使用 kebab-case (短横线分隔)
2. **标题层级**: 从 H2 (# ##) 开始，H1 由 VitePress 自动生成
3. **多语言**: 中英文文档内容要对应，保持结构一致
4. **图片存放**: 图片统一放在 `docs/public/images/` 目录

### 构建与部署

```bash
# 本地预览
npm run docs:dev

# 构建静态站点
npm run docs:build

# 预览构建结果
npm run docs:preview
```

### 注意事项

- ✅ 确保中英文文档结构保持同步
- ✅ 新增文档后更新导航配置
- ✅ 提交前先本地预览确认无误
- ❌ 不要直接修改 `docs/.vitepress/dist/` 目录内容

## 主题配置说明

### 默认主题增强配置

当前使用的默认主题已配置以下增强功能：

- ✅ **Logo 显示**: 使用 `docs/public/images/logo.png`
- ✅ **社交链接**: GitHub 链接
- ✅ **页脚信息**: 版权和许可证信息
- ✅ **搜索功能**: 本地搜索
- ✅ **编辑链接**: GitHub 编辑链接
- ✅ **最后更新时间**: 显示页面最后修改时间
- ✅ **多语言切换**: 中英文切换
- ✅ **返回顶部**: 便捷导航

### 自定义功能

项目通过客户端脚本实现自定义功能：

- `docs/.vitepress/client.js` - 客户端增强脚本

**当前状态：**
- 🎨 **默认主题**: 使用 VitePress 默认主题
- 📱 **响应式设计**: 支持移动端显示
- 🌙 **深色模式**: 支持深色/浅色主题切换

### 切换主题

1. **使用默认主题**: 删除 `docs/.vitepress/theme/` 目录
2. **使用自定义主题**: 保留 `docs/.vitepress/theme/` 目录中的文件
3. **使用社区主题**: 安装并配置相应的主题包

### 主题开发

如需进一步自定义主题，请参考：
- [VitePress 主题开发文档](https://vitepress.vuejs.org/guide/theme-introduction)
- [VitePress 组件 API](https://vitepress.vuejs.org/guide/theme-sidebar#layout-slots)

